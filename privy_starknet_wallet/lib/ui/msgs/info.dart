import 'package:flutter/material.dart';

class InfoMsg extends StatelessWidget {
  const InfoMsg({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
